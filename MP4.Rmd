---
title: "How People Use Green Transport in the Greenest Place in NYC!"
author: "Marcela Osorio, Sameera Jhunjhunwala, Teneil Rhiney"
date: "May 2nd, 2019"
output: 
  html_document:
    code_folding: hide
---
```{r setup, warning=FALSE, message=FALSE, include=FALSE}
library(tidyverse)
library(RMySQL)
library(RSQLite)
library(tidycensus)
library(sf)
library(sp)
library(leaflet)
library(dplyr)
library(devtools)
#library(wordcountaddin)
db <- dbConnect(MySQL(), 
                host = "scidb.smith.edu", 
                user = "mth292", 
                password = "RememberPi", 
                dbname = "citibike")
knitr::opts_chunk$set(connection = db, max.print = 20)
```

Word count: `r wordcountaddin::word_count()`
From our group repository [^1]

This document was rendered at `r format(Sys.time(), "%B %e, %Y at %H:%M:%S"))`

------INSERT GIF HERE---------

-----INSERT 800-1200 words WRITING HERE------

![*CitiBike*](https://citibikemiami.com/images/citibikemiamihome.png)
![*Biker in Central Park*](https://media-cdn.tripadvisor.com/media/photo-s/11/ee/e7/93/central-park.jpg)
![*Citibike station in Central Park*](https://s3.amazonaws.com/bklyner/bklyner/wp-content/uploads/2015/07/fgf_citibike_park.jpg)

```{sql, output.var = "time_square_north", connection = db, message= FALSE, warning=FALSE}
SELECT COUNT(*) AS num_trips, 
    HOUR(t.start_time) As initial_time, t.gender, 
COUNT(DISTINCT DATE(t.start_time)) AS num_days, 
COUNT(DISTINCT sm.station_id) AS num_stations,
    sm.name, sm.avg_lon, sm.avg_lat
FROM trips t
INNER JOIN station_months sm ON sm.station_id = t.start_station_id
WHERE start_station_id = 3374
AND gender <> 0 
GROUP BY initial_time, gender;
```
 
```{sql, output.var = "time_square_south", connection = db, message= FALSE, warning=FALSE}
SELECT COUNT(*) AS num_trips, 
    HOUR(t.start_time) As initial_time, t.gender, 
COUNT(DISTINCT DATE(t.start_time)) AS num_days, 
COUNT(DISTINCT sm.station_id) AS num_stations,
    sm.name, sm.avg_lon, sm.avg_lat
FROM trips t
INNER JOIN station_months sm ON sm.station_id = t.start_station_id
WHERE start_station_id = 2006
AND gender <> 0 
GROUP BY initial_time, gender;
```


```{sql, output.var = "time_square_north_south", connection = db, message= FALSE, warning=FALSE}
SELECT COUNT(*) AS num_trips, 
    HOUR(t.start_time) As initial_time, t.gender, 
COUNT(DISTINCT DATE(t.start_time)) AS num_days, 
COUNT(DISTINCT sm.station_id) AS num_stations,
    sm.name, sm.avg_lon, sm.avg_lat
FROM trips t
INNER JOIN station_months sm ON sm.station_id = t.start_station_id
WHERE start_station_id = 3374
AND end_station_id =  2006
AND gender <> 0 
GROUP BY initial_time, gender;
```

```{sql, output.var = "time_square_south_north", connection = db, message= FALSE, warning=FALSE}
SELECT COUNT(*) AS num_trips, 
    HOUR(t.start_time) As initial_time, t.gender, 
COUNT(DISTINCT DATE(t.start_time)) AS num_days, 
COUNT(DISTINCT sm.station_id) AS num_stations,
    sm.name, sm.avg_lon, sm.avg_lat
FROM trips t
INNER JOIN station_months sm ON sm.station_id = t.start_station_id
WHERE start_station_id = 2006
AND end_station_id = 3374
AND gender <> 0 
GROUP BY initial_time, gender;
```

```{sql, output.var = "sm_points", connection = db, message= FALSE, warning=FALSE}
SELECT station_id, name, avg_lat AS lat, avg_lon AS lon
  FROM station_months 
  WHERE station_id IN (3374, 2006)
  GROUP BY station_id;
```

```{r echo=TRUE, message= FALSE, warning=FALSE}
# Converting to sf
points_sf <- sm_points %>% 
  st_as_sf(coords = c("lon", "lat"))

# Creating a fancy bicycle icon
icon.fa <- makeAwesomeIcon(icon = "cycle", markerColor = "blue", library = "fa",
                           iconColor = "black")
# Creating A map of the stations 
map <- leaflet() %>%
 addTiles() %>%
 addPolylines(data = sm_points, lat = ~lat, lng = ~lon) %>%
   addAwesomeMarkers(data = points_sf, popup = ~name,
    icon = icon.fa)
 map
 
# Calculated distance between points, and found total distance by adding values in matrix
points_sf %>%
  st_distance()
```

```{r echo=TRUE, message= FALSE, warning=FALSE}
#Converting gender from a binary variable to a character variable. By having gender as a factor, we can use it to compare them in the later diagrams (as R now reads it as discrete)
time_square_north_south <- time_square_north_south %>%
  mutate(gender =  as.factor(if_else(gender == 1, "Male", "Female")))

time_square_south_north <- time_square_south_north %>%
  mutate(gender =  as.factor(if_else(gender == 1, "Male", "Female")))

time_square_south <- time_square_south %>%
  mutate(gender =  as.factor(if_else(gender == 1, "Male", "Female")))

time_square_north <- time_square_north %>%
  mutate(gender =  as.factor(if_else(gender == 1, "Male", "Female")))
```


```{r echo=TRUE, message= FALSE, warning=FALSE}
hours_2006 <- ggplot(data = time_square_south, aes(x = initial_time, y = num_trips)) +
  geom_line(aes(color = gender)) +
  geom_point(aes(color = gender))
hours_2006
```

```{r echo=TRUE, message= FALSE, warning=FALSE}
hours_3374 <- ggplot(data = time_square_north, aes(x = initial_time, y = num_trips)) +
  geom_line(aes(color = gender)) +
  geom_point(aes(color = gender))
hours_3374
```

```{r echo=TRUE, message= FALSE, warning=FALSE} 
hours3374_2006 <- ggplot(data = time_square_north_south, aes(x = initial_time, y = num_trips)) +
  geom_line(aes(color = gender)) + 
  geom_point(aes(color = gender))
hours3374_2006
```

```{r echo=TRUE, message= FALSE, warning=FALSE}
hours_2006_3374 <- ggplot(data = time_square_south_north, aes(x = initial_time, y = num_trips)) +
  geom_line(aes(color = gender)) + 
  geom_point(aes(color = gender))
hours_2006_3374
```

[^1]: https://github.com/SameeraJhunjhunwala/MP4_bikes
